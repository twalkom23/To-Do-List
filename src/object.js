//creating the object constructor
export function TaskObject(task, done, dueDate, priority, notes, category) {
    this.task = task;
    this.done = done;
    this.dueDate = dueDate;
    this.priority = priority;
    this.notes = notes;
    this.category = category;
};


